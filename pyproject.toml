[project]
name = "uv-workspace-sample"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
test = [
    "pytest>=8.4.2",
]
lint = [
    "ruff>=0.13.1",
]
type = [
    "mypy>=1.18.2",
]

dev = [
    { include-group = "test" },
    { include-group = "lint" },
    { include-group = "type" },
    "pre-commit>=4.3.0",
]
